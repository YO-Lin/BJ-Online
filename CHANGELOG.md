# 開發紀錄

這份文件記錄每次程式改動的內容，只給開發時參考，不對外顯示在網站上。

## 2026-09-10 — 分牌後的手牌改成同一座位並排顯示，不再各自長出獨立座位

- 之前分牌後，新手牌會在牌桌弧線上多長出一個全新座位格子，視覺上像多了一個玩家。使用者要求分牌後的手牌要擠在原本那一個座位範圍內並排顯示，只用同一個框。
- `server/game/roundStateMachine.js`：`placeBet()`／`split()` 建立的手牌新增 `groupId` 欄位（下注建立時等於自己的 id，split 產生的子手牌沿用原手牌的 groupId），用來標記同一組分牌血緣。`server/socket/serialize.js` 同步把 `groupId` 加進傳給前端的資料。
- 前端新增 `client/src/components/SeatGroup.tsx`：把原本 `HandView.tsx` 身兼的「座位定位＋邊框＋玩家名稱」抽出來獨立成一層，`RoomScreen.tsx` 改成依 `groupId` 分組決定座位數與位置（不分牌時跟原本行為完全一樣），同一組的手牌並排渲染在同一個 `SeatGroup` 裡，共用一個邊框、玩家名稱只顯示一次。
- 分牌後的手牌（`splitDepth > 0`）牌組改成上下小幅度重疊（不轉角度），沒分牌的單一手牌維持原本旋轉扇形攤開不變。
- 用本機 Postgres + Node server 實際跑過真實 Socket.IO 流程驗證：湊出對子分牌後，`room:state` 裡兩手牌確實共用同一個 `groupId`，其他座位的手牌各自獨立成組不受影響。TypeScript build 通過。
- 這次修改完先不部署，等使用者確認後再部署。

## 2026-09-10 — 投降改成立即結算籌碼，修正牌局紀錄缺少投降標籤的bug

- `server/game/roundStateMachine.js` 的 `surrender()`：原本只設定 status，實際扣籌碼要等整局所有玩家、莊家都行動完才在 `resolveRound()` 一次結算。改成投降當下就算出 `-Math.floor(bet/2)` 並直接回傳，同時把 `hand.result` 提早設成 `'SURRENDER'`、標記新增的 `earlySettled` 旗標，讓 `resolveRound()` 之後不會對同一手牌重複扣款。
- `server/socket/index.js`：把 `surrender` 從共用的 hit/stand/double/split actionMap 抽出來，獨立寫 `action:surrender` handler，呼叫 `applyChipDelta` 並立刻 emit `chip:update`，跟現有 `chip:topup` 用同一套模式。
- 特別處理過一個邊界情況：玩家先買保險、之後才投降這手牌——投降的半注罰款可以立即結算，但保險理賠仍要等莊家暗牌翻開才知道，所以 `resolveRound()` 對 `earlySettled` 的手牌只補算保險那部分，不會把投降罰款算兩次。已用直接呼叫引擎的方式測試過這個情境（先買保險50、投降扣50、莊家開出Blackjack、保險理賠100，總計正好+50，沒有重複扣款）。
- `client/src/components/HistoryLog.tsx` 的 `RESULT_LABEL` 原本沒有 `SURRENDER`／`EVEN_MONEY` 兩個 key，導致整局結束後右側牌局紀錄裡投降/等額支付的那行會顯示 `undefined`，這次一併補上。
- 這次修改完先不部署，等使用者確認後再部署。

## 2026-09-09 — 分牌上限從最多3次（4手）改成最多2次（3手）

- `server/game/handEngine.js` 的 `canSplit()` 條件從 `splitDepth < 3` 改成 `splitDepth < 2`。同步更新 `client/src/components/RoomScreen.tsx` 的前端判斷、README規則說明。
- 用直接呼叫引擎的方式測試過：分第1次(1→2手)、分第2次(2→3手)都成功，第3次嘗試正確被擋下。

## 2026-09-09 — 分牌不再受全房7手上限限制

- 使用者回報明明湊到對子（6,6對莊家5）卻不能分牌。排查後確認是設計上的限制：分牌需要多一個手牌欄位，之前分牌跟一般下注共用同一個「全房手牌數≤7」的上限檢查，房間手牌一滿就連分牌都會被擋下。
- 依使用者確認的規則調整：分牌的上限改成**只看這手牌自己分過幾次（最多3次）**，完全獨立於全房7手的下注上限；一般下注開新手牌還是受7手上限限制。
- 前端牌桌畫面也同步調整：座位弧線改成動態依實際手牌數（`Math.max(maxHands, 實際手牌數)`）展開，超過7手時牌桌會自動多畫出座位，不會有分出來的手牌沒地方顯示的問題。
- 用「房間先湊滿7手，再對其中一手湊出對子分牌」的情境直接測試過，分牌成功變成8手，跟舊行為（會被擋下）對照確認修正生效。

## 2026-09-09 — 加倍時顯示兩疊籌碼，不是一個籌碼顯示雙倍數字

- 玩家加倍時，下注格改成顯示兩個疊在一起的籌碼（各顯示原本的下注金額，例如原本50變成兩個50疊在一起），取代原本一個籌碼直接顯示100的做法，模擬真實賭桌加倍時「在旁邊放第二疊等額籌碼」的畫面。
- 新增 `hand.wasDoubled` 欄位（`server/game/roundStateMachine.js`），跟 `status` 分開追蹤，確保加倍後就算後來爆牌（status變成BUST），籌碼疊圖還是正確顯示兩疊，不會因為狀態改變就跳回一疊。用直接呼叫引擎測試過「加倍不爆牌」跟「加倍後爆牌」兩種情境，`wasDoubled` 都正確維持`true`。

## 2026-09-09 — 修正「分牌按鈕沒反應」：補上停用樣式

- 使用者回報按分牌沒反應。實際排查：分牌邏輯本身完全正常，用直接呼叫引擎與模擬房間手牌數已達7手上限的情境都驗證過，該擋的時候會正確擋下並顯示錯誤訊息。
- 真正原因是視覺問題：要牌/停牌/加倍/分牌/投降這排按鈕從來沒有加上「停用時」的樣式，`disabled` 按鈕看起來跟平常一模一樣，玩家點了自然沒有任何視覺回饋，容易誤以為壞掉。已補上 `.action-row button:disabled`（變灰、`cursor:not-allowed`），之後按鈕被禁用時能一眼看出來。

## 2026-09-09 — 保險／等額支付面板移到右側欄，配色簡化

- 保險跟等額支付的提示框從畫面下方的固定浮層移到右側欄（Hi-Lo算牌/牌局紀錄上方），下注/開始發牌/重置牌靴/確認下一局這些還是留在下方固定浮層。
- 配色從深咖啡色（`#4a3a0f`）改成跟側邊欄其他面板一致的深綠色（`#0f4a2c`），提示文字改金色強調，每一列改成文字與按鈕左右對齊（`justify-content: space-between`），視覺更簡潔一致。

## 2026-09-09 — 修正莊家補牌動畫瞬間出現（重複廣播蓋掉動畫）

- 使用者回報莊家補牌時後面的牌是瞬間出現，不是逐張滑入。
- 根因：伺服器在會觸發「莊家回合」的動作（如最後一位玩家停牌）裡，會廣播兩次房間狀態——一次在動作剛執行完，一次在 `settleIfDealerTurn` 結算完賠付之後，兩次廣播內容其實一樣（莊家牌都已經算完了），但前端會收到兩次 `room:state`。第二次廣播讓發牌動畫的hook誤判「這些牌已經顯示過了」，把還在等待延遲播放的牌直接取消動畫、瞬間定格在最終畫面。
- 修正：新增 `broadcastOrSettle` 統一處理，一個動作只會廣播一次房間狀態（如果動作讓回合進入莊家階段，就只讓 `settleIfDealerTurn` 結算完後廣播那一次，不再多廣播一次）。用計算「動作觸發後收到幾次room:state」的方式驗證過，修正後正確變成剛好1次。

## 2026-09-09 — 修正大廳籌碼顯示是過期的快取數字

- 使用者回報：一開始打開連結（已登入帳號），籌碼固定顯示1000，按儲值後才「跳成原本數字+1000」。
- 根因：伺服器原本只有在「加入房間」時才會把資料庫裡真實的籌碼傳給前端，在那之前畫面顯示的是登入當下寫進 localStorage 的舊數字，玩過幾局或儲值過後這個快取數字就跟資料庫實際值不一致了。
- 修正：`server/socket/index.js` 在 socket 一連線（還沒加入任何房間）就立刻查真實籌碼並送 `chip:update`，不用等到加入房間才更新，大廳畫面顯示的籌碼永遠是即時的資料庫數字。
- 用「儲值改變真實餘額→斷線→用同一組token重新連線→確認一連上就顯示正確餘額（不用加房間）」的方式驗證過。

## 2026-09-09 — 莊家補牌動畫再放慢、加上明顯停頓感

- 莊家自己補牌（暗牌＋不足17點時的加牌）的間隔從 950ms 拉長到 1500ms，單張滑入動畫時長從 650ms 拉長到 800ms，兩者相減後每張牌完全滑入定住後、到下一張開始滑入之間會有約 700ms 的明顯停頓，比較有一張一張揭曉的緊張感。

## 2026-09-09 — 下注/保險/等額支付面板改成固定浮層，不再擠壓牌桌

- 保險（最多7列）跟等額支付的提示框內容量差異很大，之前這些區塊是排在牌桌下方的正常排版流程裡，內容一多就把牌桌往上推、整個畫面變形。
- 改成用 `position: fixed` 固定在畫面最下方的浮動面板（`.action-overlay`），完全脫離牌桌所在的排版流程，不管裡面顯示多少列，牌桌本身的大小與位置永遠固定不動。內容太多時面板自己內部捲動（`max-height: 42vh; overflow-y: auto`）。

## 2026-09-09 — 修正舊帳號籌碼歸零後卡死：新增大廳儲值功能

- 排查使用者回報「已登入帳號建新房後無法下注」：實際原因不是bug，是該帳號籌碼真的是0（下注按鈕正確地擋住超過餘額的下注）。但發現一個真正的功能缺口——新註冊帳號可以自選起始籌碼，但既有帳號一旦籌碼歸零就永久卡住，沒有任何補充籌碼的管道。
- 新增大廳「補充籌碼」功能：玩家可自行輸入想儲值的金額（100~1,000,000之間的整數），新增 `chip:topup` socket事件（`server/socket/index.js`，複用 `usersRepo.js` 既有的範圍常數與 `applyChipDelta`）。
- 用一個真的歸零籌碼的測試帳號完整驗證過：登入顯示0→儲值1000→餘額變1000→成功下注，整個流程正確。

## 2026-09-09 — 新增 Even Money（等額支付）

- 玩家一發牌是天生 Blackjack，且莊家明牌是 A 或 10 點牌時，可以選擇先拿 1:1 的等額支付，不用等莊家開牌賭 3:2 還是平手。
- 跟真實賭場的標準規則不同：標準規則只有莊家明牌是 A 才有 Even Money（因為機制上等同幫自己的 Blackjack 買保險），這裡依需求也開放莊家明牌是 10 點牌的情況（因為本作暗牌本來就要等大家都行動完才發，玩家一樣要冒平手的風險，開放對稱處理）。
- 新增 `evenMoney:decide` socket 事件、`EVEN_MONEY` 回合階段（莊家明牌是10點牌、且有玩家天生Blackjack時觸發；莊家明牌是A則沿用原本的INSURANCE階段，兩種決定並存）、`EVEN_MONEY` 手牌狀態（拿了就直接以 `payout = bet` 結算，不管莊家實際牌況如何）。
- 用直接呼叫引擎的方式測試過：莊家明牌A時拿等額支付、不拿等額支付但莊家也開出Blackjack（驗證平手0元 vs 等額支付200元的落差）、莊家明牌10點牌時的等額支付、以及多人房間裡一人是Blackjack要決定等額支付、另一人是一般手牌要決定保險同時並存的情境，都正確。

## 2026-09-08 — 莊家補牌動畫再放慢、註冊時可自選起始籌碼

- 莊家自己補牌（暗牌＋不足17點時的加牌）動畫間隔從 650ms 再拉長到 950ms，比單張動畫本身的時長（650ms）更長，確保是「一張完全播完才播下一張」而不是疊在一起。
- 註冊帳號時新增「起始籌碼」欄位，可自行輸入想要的起始籌碼數（預設1000），伺服器端限制在100~1,000,000之間的整數，超出範圍或非整數會擋掉。登入既有帳號不會再問（籌碼已經是帳號本身的持久資料）。

## 2026-09-08 — 新增投降 (Surrender) 功能

- 新增真正的投降動作：`server/game/handEngine.js` 的 `canSurrender()`（跟加倍用同一套資格判斷：只有手上剛好是最初兩張牌時才能投降）、`server/game/roundStateMachine.js` 的 `surrender()` 動作（手牌狀態變成 `SURRENDERED`，結算時拿回一半籌碼、輸掉另一半，即 `payout = -floor(bet/2)`）、`server/socket/index.js` 新增 `action:surrender` socket事件。
- 前端 `HandView.tsx` 在操作按鈕列加了「投降」按鈕，跟「加倍」共用同一個可用條件（只有最初兩張牌時才能按）；新增 `SURRENDERED`/`SURRENDER` 的狀態與結果文字標籤。
- 上一版策略表加的「RH」提示現在名副其實了——玩家真的可以照建議按下投降。
- README.md 規則說明同步更新（原本寫「不可投降」「莊家Peek」，已經都過時，改成正確的「可投降（僅限最初兩張牌）」「莊家無提前偷看」）。

## 2026-09-08 — 策略表調整：15/16點對莊家9、10改成RH（僅限最初兩張牌）

- `server/game/basicStrategy.js`：硬15點對莊家10、硬16點對莊家9和10，策略建議從單純的H改成RH（Surrender if allowed, else Hit）。這個遊戲沒有開放投降選項，所以RH純粹是提示文字（告訴玩家理論最佳解是投降，但因為不能投降所以照打要牌），不影響實際可操作的動作按鈕。
- 修正：RH只在玩家手上剛好是最初發的兩張牌時才會顯示（投降本來就只在第一次決策時有意義），要牌後即使湊出15或16點，提示會正確顯示成單純的H。

## 2026-09-08 — 莊家補牌動畫放慢、輸贏籌碼動畫

- 莊家自己補牌（暗牌＋不足17點時的加牌）原本會一次全部跳出來，因為伺服器端是同步一次算完才廣播。修正 `useDealAnimation`：只要同一次更新裡一次新增超過1張牌（莊家補牌的情況），就會逐張錯開時間播放（間隔約650ms）；玩家自己要牌（一次只會新增1張）維持立即出現，不受影響。
- 新增輸贏籌碼動畫：玩家輸的那一局，下注籌碼會有「被收走」的滑落淡出動畫；贏的那一局，會在原本籌碼旁多一個「+贏得金額」的綠色籌碼滑入動畫。資料來源是 `round:result` socket 事件裡的 `payout`（正負決定播哪種動畫），只在結算階段短暫顯示。

## 2026-09-08 — 文字與撲克牌圖片再放大

- 撲克牌圖片：基礎尺寸與座位扇形牌都再調大一輪。
- 房間資訊列、下注格籌碼、座位資訊面板（暱稱/點數/狀態）、操作按鈕、策略提示、保險提示、側邊欄（算牌/歷史紀錄）、玩家列表標籤等文字大小都調大了。

## 2026-09-08 — 發牌動畫改成真正的輪流發牌順序

- 之前發牌動畫雖然伺服器邏輯已經是「各家發第一張→莊家→各家發第二張」，但動畫呈現時是每位玩家的兩張牌背靠背一起播放完才換下一位，視覺上看起來像「每人直接發兩張」，跟實際規則不符。
- 改成動畫也照真正的輪流順序播放：先播所有玩家的第一張牌（依序），再播莊家明牌，最後才播所有玩家的第二張牌（依序）。`useDealAnimation` 的參數從單一數字改成「每張牌各自的出場順序」陣列，讓呼叫端能精確指定每張牌在整個發牌序列中的位置。

## 2026-09-08 — 修正發牌動畫延遲計算錯誤

- 修正一個 bug：發牌動畫的延遲公式原本對「要牌/加牌後新出現的那張牌」也套用了跟「初始發牌」一樣的座位序號放大延遲，導致要牌後新牌要等非常久（座位越後面、牌越多局，delay 甚至長達幾十秒）才會真的滑入畫面，看起來像是卡住、要等很久或觸發別的動作才會出現。
- 修正後只有「這手牌/莊家從 0 張牌變成有牌」的那一刻（也就是真正的初始發牌）才會套用座位序號的滑入排序；之後任何新牌（要牌、加倍、分牌、莊家補牌）一律立刻滑入，不再排隊等待。

## 2026-09-08 — 賭桌寬度/牌面尺寸調整、下注輸入框修正、發牌流程改成正統順序

- 桌面 `aspect-ratio` 改回 1.6/1（保留正圓的橢圓計算方式，只是比例變寬），橫向空間變多。
- 玩家與莊家的牌面圖片都調大，座位欄寬與下注格也配合放大，避免變大後互相擠在一起。
- 下注金額輸入框改用字串暫存打字內容（`type="text"` + `inputMode="numeric"`），修正原本 `type="number"` 搭配數字 state 在清空重打時會卡頓/跳動的問題。
- 發牌動畫再放慢一階（每張牌間隔 350ms→550ms，單張動畫 450ms→650ms，滑入距離 -40px→-70px）。
- **發牌流程改成正統順序**：依序發給每位玩家 1 張牌 → 莊家發給自己 1 張明牌 → 依序發給每位玩家第 2 張牌 → 莊家逐位詢問玩家動作（不加牌才問下一位）→ 所有玩家行動完畢後，莊家才補發第二張牌（暗牌）並視 17 點規則繼續要牌。保險機制保留（莊家明牌 A 時可下注），但**不再提前偷看莊家暗牌**，莊家有沒有 21 點要等最後大家都行動完、暗牌揭曉才結算，不會像以前一樣提前結束整個回合。

## 2026-09-07 — 移除多餘弧線、座位間距加大

- 移除桌面中間那條跟座位方向不一致的橘色弧線裝飾（視覺上會被誤認成倒U，直接拿掉）。
- 縮小座位格、扇形牌與資訊面板的尺寸，讓 7 個座位在較窄的圓形桌面下也不會互相擠在一起。

## 2026-09-07 — 弧度方向修正、整頁滿版、改圓形賭桌實驗

- 座位弧線方向修正成 U 形（中間座位離玩家最近、兩端離莊家最近），修正前一版做反的倒 U（∩）方向。
- 整個房間畫面改成固定塞進一個視窗高度（`100svh`），賭桌區塊用 flex 吃掉剩餘空間、側邊欄歷史紀錄改成內部捲動，不需要整頁下拉捲動。
- 賭桌形狀從扁橢圓形改成正圓形（實驗性調整，看使用者回饋再決定要不要保留）。

## 2026-09-07 — 依實拍賭桌照片重新設計座位、座位順序改右到左

- 每個座位改成「上方扇形牌 + 下注方格籌碼 + 下方暱稱/點數/操作按鈕」的三段式呈現，取代原本整個手牌塞在一個大框裡的做法，比照真實賭桌空拍照的樣子。
- 加上一條實際可見的金色弧線連接所有座位，「Dealer must stand on 17 and must draw to 16」與「INSURANCE PAYS 2 TO 1」文字沿弧線排列。
- 座位順序改成從右到左（第一個下注的玩家在最右邊，後面依序往左排）。
- 賭桌容器改成允許內容溢出（避免座位下方的資訊面板被裁切）。

## 2026-09-07 — 移除更新日誌頁面、賭桌視覺微調、發牌流程調整

- 移除「更新日誌」頁面與相關 API（`/api/changelog`）、資料庫存取層、CLI 工具；改用這份 `CHANGELOG.md` 記錄開發歷程，不再對玩家開放查詢。
- 賭桌視窗加大、改成更圓弧（橢圓）的外型；座位格與手牌框不再跟著弧度旋轉，一律保持正向。
- 發牌動畫改為固定的座位優先順序：所有玩家的手牌全部發完後，才開始發莊家的牌（原本的實作在特定情況下會讓莊家先動畫，已修正）。
- 發牌動畫的間隔放慢（每張牌間隔從 150ms 拉長到 350ms，單張動畫時長從 280ms 拉長到 450ms）。
- 每局結算後不再自動立刻回到下注畫面，改成顯示「確認，開始下一局」按鈕，玩家確認後才進入下一局（新增 `round:continue` socket 事件）。

## 2026-09-07 — 賭桌視覺改版

- 換上真實撲克牌圖片取代原本 CSS 畫的牌面。
- 新增弧形賭桌桌布背景，7 個座位沿弧線排列（對應真實 21 點桌的下注格），弧線上有彎曲的 SVG 文字（BLACK JACK / PAYS 3 TO 2 / Dealer must stand on 17...）。
- 新增發牌動畫，牌會依序滑入畫面。
- Hi-Lo 流水數/真數與牌局紀錄移到畫面右側欄位。
- 全房手牌上限從 6 手提高到 7 手（對應桌布的 7 個下注格）。

## 2026-09-07 — 修正卡在「連線中」的問題

- Socket 連線物件原本存在 `useRef`，而 React 改變 ref 不會觸發重新渲染，導致連線建立後畫面沒有更新、卡在「連線中...」。改成存在 `useState`。
- 修正 Zeabur 的 Postgres add-on 沒有 `DATABASE_URL` 這個變數名稱的問題（改用 `POSTGRES_CONNECTION_STRING`/`POSTGRES_URI` 當備援）。

## 2026-09-07 — 部署到 Zeabur

- 加上 `Dockerfile`／`.dockerignore`，繞過 Zeabur 自動偵測誤判成 yarn 專案的問題。
- 建立 GitHub repo（YO-Lin/BJ-Online）、Zeabur 專案（Node service + Postgres addon）。
- 正式站上線：https://bj-online.zeabur.app/

## 2026-09-06 — 新增更新日誌頁面（已於後續版本移除，見上方）

## 2026-09-06 — 初版完成

- 多人連線 21 點：React+Vite 前端、Node+Express+Socket.io 後端、Postgres 存帳號與籌碼。
- 6 副牌連續靴、S17、DAS、不可投降、莊家 Peek、保險 2:1、Blackjack 賠 3:2。
- 最多 6 位玩家同房（後續版本改為對應 7 個下注格）、每人可開多手、Hi-Lo 算牌（共享流水數/真數）、基本策略提示（僅限本人手牌）、牌局歷史紀錄、重置牌靴。
